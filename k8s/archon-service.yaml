apiVersion: v1
kind: Service
metadata:
  name: archon
  namespace: archon
  labels:
    app: archon
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    app: archon
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
